---
const nav = [
  { href: '/work', label: 'Work' },
  { href: '/open-source', label: 'Open Source' },
  { href: '/about', label: 'About' },
];

const path = Astro.url.pathname.replace(/\/$/, '') || '/';
---

<header class="on-night bg-night">
  <div
    class="mx-auto flex max-w-5xl flex-wrap items-baseline justify-between gap-x-6 gap-y-1 px-5 py-5 sm:px-8"
  >
    <a
      href="/"
      class="font-display text-base font-semibold tracking-tight text-bone hover:text-glow-soft"
    >
      Sometimes Creek<span class="text-glow">&nbsp;Ventures</span>
    </a>
    <nav aria-label="Main">
      <ul class="flex items-baseline gap-5 text-sm sm:gap-7">
        {
          nav.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={
                  path === item.href || path.startsWith(item.href + '/')
                    ? 'page'
                    : undefined
                }
                class:list={[
                  'hover:text-glow-soft',
                  path === item.href || path.startsWith(item.href + '/')
                    ? 'font-medium text-glow'
                    : 'text-mist',
                ]}
              >
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
